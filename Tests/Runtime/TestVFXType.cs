namespace WTFGames.Hephaestus.VFX.Tests
{
    /// <summary>
    /// Stands in for the enum a project exports from its VFXLibraryConstants.
    /// </summary>
    public enum TestVFXType : byte
    {
        SPARK = 0,
        SMOKE = 5,
        MISSING = 42
    }
}
